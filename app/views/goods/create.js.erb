$("#review-<%=@review.id%>_good").html("<%= j(render partial: 'goods/good-bad', locals: { review: @review }) %>");
$('#alert-message').html('<div class="alert alert-success mt-1 text-center auto-hide"><%= @msg %></div>');
$(function(){
    setTimeout("$('.auto-hide').fadeOut('slow')", 1500)
  });

$("#self_review-<%=@review.id%>_good").html("<%= j(render partial: 'goods/good-bad', locals: { review: @review }) %>");
