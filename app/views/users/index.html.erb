<div class="main users-index">
  <div class="container">
    <h1 class="users-heading">ユーザー一覧</h1>
    <% @users.each do |user| %>
      <div class="users-index-item">
        <div class="user-left">
          <img src="<%= user.image_id %>" >
        </div>
        <div class="user-right">
          <%= link_to(user.name, "/users/#{user.id}") %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div class="card" style="width: 18rem;">

  <div class="card-body">
    <div class="card-title">
      <% @users.each do |user| %>
      <div class="users-index-card">
        <div class="user-left">
          <img src="<%= user.image_id %>" >
        </div>
          <div class="user-right">
          <%= link_to(user.name, "/users/#{user.id}") %>
        </div>
        </div>
    </div>


    <div class="card-text user-data-count">
      <div class="count-box">
        <span class="card-label">Followers</span>
        <span class="card-count">12332</span>
      </div>
      <div class="count-box">
        <span class="card-label">Followings</span>
        <span class="card-count">1432</span>
      </div>
      <div class="follow-box">
        <button type="button" class="btn btn-outline-dark btn-sm">フォロー</button>
      </div>
      <div class="user-intro">
        <span>紹介文</span>
    　  <span><%= user.intro %></spanp>
    　</div>
    </div>
  </div>
</div>
<% end %>